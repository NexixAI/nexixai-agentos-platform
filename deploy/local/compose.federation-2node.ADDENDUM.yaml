services:
  federation-e2e:
    build:
      context: ../..
      dockerfile: tests/federation_e2e/Dockerfile.e2e
    depends_on:
      - nodea-agent-orchestrator
      - nodea-model-policy
      - nodea-federation
      - nodeb-agent-orchestrator
      - nodeb-model-policy
      - nodeb-federation