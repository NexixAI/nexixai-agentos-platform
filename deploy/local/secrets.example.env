# Example placeholders only — replace with real values via secret manager or per-env files.
# Do NOT commit real secrets.
AGENTOS_SERVICE_TOKEN=fake_service_token_local
AGENTOS_FED_SHARED_SECRET=fake_federation_secret_local
AGENTOS_SIGNING_KEY=fake_signing_key_material
