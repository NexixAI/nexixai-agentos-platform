apiVersion: 1

providers:
  - name: agentos
    orgId: 1
    folder: AgentOS
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
